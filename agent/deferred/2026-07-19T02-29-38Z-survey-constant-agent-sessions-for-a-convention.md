# Survey `constant`'s `agent/sessions/` and consider a sessions convention

While surveying `constant` for the plans/design/experiments conventions (2026-07-18), a fourth artifact directory was noticed and **not** characterized: `constant`'s `agent/sessions/`. It may warrant its own Agent Norms convention — a foundation *directory* rule (what `agent/sessions/` holds, naming) and a `docs` *writing* rule (how a session record reads) — parallel to the plans/design/experiments rules just authored.

Nothing is known about its contents yet; the survey was deliberately not done.

**Gated on:** finishing this project's (Agent Norms) current work first. Per Scott's direction (2026-07-18), **no `constant` work at the moment** — return to `constant` only after the agent-norms work in flight is done. Surveying `agent/sessions/` means reading `constant`, so it waits.

**Why:** the lead is worth keeping so a fourth artifact convention isn't forgotten, but acting on it now would mean `constant` work Scott has explicitly deferred. A parked item keeps the lead without pulling focus off agent-norms.

**How to apply:** once the agent-norms work is complete and `constant` work is back on the table, survey `constant`'s `agent/sessions/` for its de-facto conventions, then decide whether a `sessions` convention is warranted (foundation directory rule + `docs` writing rule) or whether it's out of scope. Related: the agent-experiments-convention, the agent-plans-convention, and the agent-design-convention (the sibling artifact conventions).
