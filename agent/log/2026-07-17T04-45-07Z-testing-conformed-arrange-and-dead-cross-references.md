# **Ran the dead-cross-reference and conformance pass on `testing`** — the pass that ran on `code/ruby` at publication but never on `testing`. Two defect classes found and fixed. **(1) Unresolvable cross-references.** `testing` reached *up* into `design-by-efferent` rules (`tdd-test-structure` → "the TDD-as-design-tool rule, the TDD first-turn rule"; `test-block-is-assertion-only` → "the first-turn rule"; `say-verified-not-green` and `say-protect-not-guard` → "the lexicon rules") and *sideways* into `code/ruby` (`test-block-is-assertion-only` → the no-inline-method-call-arguments rule, by dead `agent/rules/code/…` path). `testing` includes only `foundation` and `vocabulary`, so none of these resolve for a consumer — and the DBE names were stale as of the genus-test relabeling. The upward references lose nothing when dropped: DBE's rules already point *down* to testing's, so the linkage exists from the correct side. **(2) `testing` violated its own published rule.** `controls-not-factories-fixtures-arrange` bans "arrange", while `tdd-test-structure` said "Arrange and actuate once" / "a shared arrangement-and-actuation block" and `context-only-for-local-instrumentation` said "the shared arrangement and actuation" — now **control** throughout. Also dropped `tdd-test-structure`'s `test/automated/define_constant.rb` exemplar, a `constant` path no consumer has. **Not touched:** illustrative `test/automated/…` example paths in three rules, which are examples rather than cross-references. **`testing` now requires a re-publish** (Phase B, gated).
